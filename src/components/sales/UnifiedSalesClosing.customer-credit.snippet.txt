Integration target discovered in main UnifiedSalesClosing: the file defines its own CustomerCreditSalesSource around lines 368–470 and renders it inside the Dynamic Sales Sources section. The selector currently embeds `Debt` and `Available` amounts in each option and duplicates outstanding debt in the payment card. Replace only that local component with the extracted compact component, keeping its existing props and callbacks. The existing main file's canonical customer query uses `loadCanonicalActiveCustomers()` backed by `erp_list_customer_credit_options`; this RPC aggregates outstanding from debt_records. Closing persistence uses saveClosingSession / erp_save_sales_closing and stores credit_entries_json. Direct debt payment uses recordCustomerReceivablePayment / erp_record_customer_receivable_payment.